const express = require('express');
const pgroutr = express.Router();
const Usrchma = require('../models/usrschema');


pgroutr.post('/profile', function(req, res){

  const email = request.body.userna;
  const passw = request.body.blogtitle;
  const usrnm = request.body.dateToday;
  const over18 = request.body.checkbox2;

});
